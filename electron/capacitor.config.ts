import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nitinduseja',
  appName: 'Routine',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
